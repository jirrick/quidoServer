// customize values and save as 'config.js'
const config = {
    //board config
    boards: [
        {
            name: 'name',
            mac: 'mac'
        }
    ],

    //mongoDB connection string
    mongoDB: 'mongodb://...'
};
module.exports = config;